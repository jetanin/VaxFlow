services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: medcast
      POSTGRES_PASSWORD: medcast
      POSTGRES_DB: medcast
    ports:
      - "5432:5432"          # เปิดให้ backend แบบ local (npm run dev) เชื่อมได้
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U medcast"]
      interval: 5s
      timeout: 5s
      retries: 10

  adminer:
    image: adminer:latest
    restart: unless-stopped
    depends_on:
      - db
    ports:
      - "8081:8080"          # UI จัดการฐานข้อมูล: http://localhost:8081

  backend:
    build: ./backend
    environment:
      PGHOST: db
      PGPORT: 5432
      PGUSER: medcast
      PGPASSWORD: medcast
      PGDATABASE: medcast
      SEED_DIR: /seed
      PORT: 4000
      RESEED_TOKEN: medcast-reseed   # token ภายในสำหรับ retrainer เรียก /api/reseed
    volumes:
      # mount ข้อมูล + weight (read-only) ให้ backend seed ลง Postgres
      - ../data:/seed/data:ro
      - ../models:/seed/models:ro
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "4000:4000"

  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - "8080:80"

  # เทรน FedAvg ใหม่อัตโนมัติทุกวัน -> อัปเดต weight + snapshot -> สั่ง backend reseed
  retrainer:
    build: ./retrainer
    environment:
      BACKEND_URL: http://backend:4000
      RESEED_TOKEN: medcast-reseed
      DP_SIGMA: "0.1"
    volumes:
      - ..:/repo            # ทั้ง repo (อ่าน data/, เขียน models/ + data/predictions/)
    depends_on:
      - backend

volumes:
  pgdata:
